kind: Service
apiVersion: v1
metadata:
  # Service name
  name: vgeoip-app-svc
spec:
  selector:
    app: voyagin-prod
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: LoadBalancer
